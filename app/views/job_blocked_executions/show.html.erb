<% content_for(:title, @job_blocked_execution) %>

<%= turbo_stream_from(@job_blocked_execution) %>

<% if @job_blocked_execution.id.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".id") %></div>
    <div class="font-bold"><%= @job_blocked_execution.id %></div>
  </div>
<% end %>

<% if @job_blocked_execution.job.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".job") %></div>

    <% if can?(:show, @job_blocked_execution.job) %>
      <div class="font-bold">
        <%= link_to(
          @job_blocked_execution.job,
          [*nested(job: nil), @job_blocked_execution.job],
          class: "link"
        ) %>
      </div>
    <% else %>
      <div class="font-bold"><%= @job_blocked_execution.job %></div>
    <% end %>
  </div>
<% else %>
  <div class="p">
    <div class="text-gray-600"><%= t(".job_id") %></div>
    <div class="font-bold"><%= @job_blocked_execution.job_id %></div>
  </div>
<% end %>

<% if @job_blocked_execution.queue_name.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".queue_name") %></div>
    <div class="font-bold"><%= @job_blocked_execution.queue_name %></div>
  </div>
<% end %>

<% if @job_blocked_execution.concurrency_key.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".concurrency_key") %></div>
    <div class="font-bold"><%= @job_blocked_execution.concurrency_key %></div>
  </div>
<% end %>

<% if @job_blocked_execution.priority.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".priority") %></div>
    <div class="font-bold"><%= @job_blocked_execution.priority %></div>
  </div>
<% end %>

<% if @job_blocked_execution.expires_at.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".expires_at") %></div>
    <div class="font-bold">
      <%= local_time(@job_blocked_execution.expires_at) %>
    </div>
  </div>
<% end %>

<% if @job_blocked_execution.updated_at.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".updated_at") %></div>
    <div class="font-bold">
      <%= local_time(@job_blocked_execution.updated_at) %>
    </div>
  </div>
<% end %>

<% if @job_blocked_execution.created_at.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".created_at") %></div>
    <div class="font-bold">
      <%= local_time(@job_blocked_execution.created_at) %>
    </div>
  </div>
<% end %>

<div class="p">
  <%= link_to(t(".edit"), edit_url, class: "link") %>
  <%= render("shared/confirm", text: t(".destroy"), url: destroy_url) %>
  <%= render("shared/confirm", text: t(".delete"), url: delete_url) %>
</div>
