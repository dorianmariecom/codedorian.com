<div class="p">
  <% parents = nested.compact %>
  <% query = parents.to_h { |parent| ["#{parent.model_name.singular_route_key}_id", parent.to_param] } %>
  <%= link_to([submission_schedule, **query], class: :link) do %>
    <%= submission_schedule %>
  <% end %>
</div>
