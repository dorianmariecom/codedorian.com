<%= turbo_stream_from(@job_pauses) %>

<% if can?(:create, JobPause) %>
  <div class="p"><%= link_to(t(".new"), new_url, class: "button") %></div>
<% end %>

<%= render("shared/search") %>

<turbo-frame id="search" target="_top">
  <% if @job_pauses.any? &&
    (can?(:destroy_all, JobPause) || can?(:delete_all, JobPause)) %>
    <div class="p p--flex">
      <% if can?(:destroy_all, JobPause) %>
        <%= render("shared/confirm", url: destroy_all_url, text: t(".destroy_all")) %>
      <% end %>
      <% if can?(:delete_all, JobPause) %>
        <%= render("shared/confirm", url: delete_all_url, text: t(".delete_all")) %>
      <% end %>
    </div>
  <% end %>

  <%= render_collection(
    partial: "job_pauses/job_pause",
    collection: @job_pauses,
    as: :job_pause
  ) %>
</turbo-frame>
