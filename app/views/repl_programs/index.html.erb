<div class="p"><%= link_to t(".new"), new_url, class: "button" %></div>

<% if @repl_programs.any? %>
  <div class="p">
    <%= button_to t(".delete_all"), url, method: :delete, class: "button button--link" %>
  </div>
<% end %>

<%= render_collection partial: "repl_programs/repl_program",
                  collection: @repl_programs,
                  as: :repl_program %>
