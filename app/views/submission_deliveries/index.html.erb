<%= turbo_stream_from(@submission_deliveries) %>

<% if can?(:create, SubmissionDelivery) %>
  <div class="p p--flex">
    <%= link_to(t(".new"), new_url, class: "button") %>
  </div>
<% end %>

<%= render("shared/search") %>

<turbo-frame id="search" target="_top">
  <% if @submission_deliveries.any? && can?(:destroy_all, SubmissionDelivery) %>
    <div class="p p--flex">
      <%= render("shared/confirm", url: destroy_all_url, text: t(".destroy_all")) %>
      <%= render("shared/confirm", url: delete_all_url, text: t(".delete_all")) %>
    </div>
  <% end %>

  <%= render_collection(
    partial: "submission_deliveries/submission_delivery",
    collection: @submission_deliveries,
    as: :submission_delivery,
    locals: {
      nested: [@submission, @submission_section].compact
    }
  ) %>
</turbo-frame>
