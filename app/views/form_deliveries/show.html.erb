<% content_for(:title, @form_delivery) %>

<%= turbo_stream_from(@form_delivery) %>

<% if can?(:update, @form_delivery) %>
  <% if @form_delivery.id.present? %>
    <div class="p">
      <div class="text-gray-600"><%= t(".id") %></div>
      <div class="font-bold"><%= @form_delivery.id %></div>
    </div>
  <% end %>
<% end %>

<% if @form_delivery.locale.present? && can?(:update, @form_delivery) %>
  <div class="p">
    <div class="text-gray-600"><%= t(".locale") %></div>
    <div class="font-bold"><%= @form_delivery.locale %></div>
  </div>
<% end %>

<% if @form_delivery.name.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".name") %></div>
    <div class="content font-bold">
      <%= simple_format(@form_delivery.name, {}, wrapper_tag: :div) %>
    </div>
  </div>
<% end %>

<% if @form_delivery.label.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".label") %></div>
    <div class="content font-bold">
      <%= simple_format(@form_delivery.label, {}, wrapper_tag: :div) %>
    </div>
  </div>
<% end %>

<% if @form_delivery.description.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".description") %></div>
    <div class="content">
      <%= simple_format(@form_delivery.description, {}, wrapper_tag: :div) %>
    </div>
  </div>
<% end %>

<% if @form_delivery.position.present? && can?(:update, @form_delivery) %>
  <div class="p">
    <div class="text-gray-600"><%= t(".position") %></div>
    <div class="font-bold"><%= @form_delivery.position %></div>
  </div>
<% end %>

<% if can?(:update, @form_delivery) %>
  <% if @form_delivery.updated_at.present? %>
    <div class="p">
      <div class="text-gray-600"><%= t(".updated_at") %></div>
      <div class="font-bold">
        <%= local_time(@form_delivery.updated_at, format: :default) %>
      </div>
    </div>
  <% end %>

  <% if @form_delivery.created_at.present? %>
    <div class="p">
      <div class="text-gray-600"><%= t(".created_at") %></div>
      <div class="font-bold">
        <%= local_time(@form_delivery.created_at, format: :default) %>
      </div>
    </div>
  <% end %>

  <div class="p">
    <%= link_to(t(".edit"), edit_url, class: :link) %>
    <%= render("shared/confirm", text: t(".destroy"), url: destroy_url) %>
    <%= render("shared/confirm", text: t(".delete"), url: delete_url) %>
  </div>
<% end %>

<% if can?(:index, Version) %>
  <div class="p font-bold">
    <%= link_to(t(".versions"), [*nested, @form_delivery, :versions], class: :link) %>
  </div>

  <% if can?(:create, Version) %>
    <div class="p">
      <%= link_to(
        t(".new_version"),
        [:new, *nested, @form_delivery, :version],
        class: "button"
      ) %>
    </div>
  <% end %>

  <%= render_collection(
    partial: "versions/version",
    collection: @versions,
    as: :version,
    locals: {
      nested: [*nested, @form_delivery]
    }
  ) %>
<% end %>

<% if can?(:index, Log) %>
  <div class="p font-bold">
    <%= link_to(t(".logs"), [*nested, @form_delivery, :logs], class: :link) %>
  </div>

  <% if can?(:create, Log) %>
    <div class="p">
      <%= link_to(t(".new_log"), [:new, *nested, @form_delivery, :log], class: "button") %>
    </div>
  <% end %>

  <%= render_collection(
    partial: "logs/log",
    collection: @logs,
    as: :log,
    locals: {
      nested: [*nested, @form_delivery]
    }
  ) %>
<% end %>
