<%= turbo_stream_from(@job_recurring_executions) %>

<% if can?(:create, JobRecurringExecution) %>
  <div class="p"><%= link_to(t(".new"), new_url, class: "button") %></div>
<% end %>

<%= render("shared/search") %>

<turbo-frame id="search" target="_top">
  <% if @job_recurring_executions.any? && (can?(:destroy_all, JobRecurringExecution) || can?(:delete_all, JobRecurringExecution)) %>
    <div class="p p--flex">
      <% if can?(:destroy_all, JobRecurringExecution) %>
      <%= render("shared/confirm", url: destroy_all_url, text: t(".destroy_all")) %>
      <% end %>
      <% if can?(:delete_all, JobRecurringExecution) %>
      <%= render("shared/confirm", url: delete_all_url, text: t(".delete_all")) %>
      <% end %>
    </div>
  <% end %>

  <%= render_collection(
    partial: "job_recurring_executions/job_recurring_execution",
    collection: @job_recurring_executions,
    as: :job_recurring_execution
  ) %>
</turbo-frame>
