<% Current::BG_ENVS.each do |env, bg_env| %>
  <div class="p"><%= env %></div>
  <div class="p w-96 h-96 flex items-center justify-center border">
    <div class="w-80 h-80 <%= bg_env %> rounded-full"></div>
  </div>
<% end %>
