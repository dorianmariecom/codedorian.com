<% if @executions.any? %>
  <div class="p">
    <%= button_to t(".delete_all"), url, method: :delete, class: "underline" %>
  </div>
<% end %>

<%= render partial: "executions/execution", collection: @executions, as: :execution %>
