<% content_for(:title, @plan_field) %>
<% content_for(:description, @plan_field.name) %>
<%= turbo_stream_from(@plan_field) %>
<%= render("shared/attribute", label: t(".id"), value: @plan_field.id) %>
<%= render(
  "shared/attribute",
  label: t(".plan_id"),
  value: @plan_field.plan,
  type: :record
) %>
<%= render("shared/attribute", label: t(".key"), value: @plan_field.key) %>
<%= render(
  "shared/attribute",
  label: t(".name_en"),
  value: @plan_field.name_en,
  type: :content
) %>
<%= render(
  "shared/attribute",
  label: t(".name_fr"),
  value: @plan_field.name_fr,
  type: :content
) %>
<%= render("shared/attribute", label: t(".kind"), value: @plan_field.kind) %>
<%= render("shared/attribute", label: t(".required"), value: @plan_field.required?) %>
<%= render("shared/attribute", label: t(".position"), value: @plan_field.position) %>
<div class="p p--flex">
  <% if can?(:update, @plan_field) %>
    <%= link_to(t(".edit"), edit_url, class: :link) %>
  <% end %>
  <% if can?(:destroy, @plan_field) %>
    <%= render("shared/confirm", text: t(".destroy"), url: destroy_url) %>
    <%= render("shared/confirm", text: t(".delete"), url: delete_url) %>
  <% end %>
</div>
<% if can?(:index, Version) %>
  <div class="p font-bold"><%= t(".versions") %></div>
  <% if can?(:create, Version) %>
    <div class="p">
      <%= link_to(
        t(".new_version"),
        { controller: :versions, action: :new, plan_field_id: @plan_field.id },
        class: "button"
      ) %>
    </div>
  <% end %>
  <%= render_collection(
    partial: "versions/version",
    collection: @versions,
    as: :version,
    locals: {
      nested: []
    }
  ) %>
<% end %>
<% if can?(:index, Log) %>
  <div class="p font-bold"><%= t(".logs") %></div>
  <% if can?(:create, Log) %>
    <div class="p">
      <%= link_to(
        t(".new_log"),
        { controller: :logs, action: :new, plan_field_id: @plan_field.id },
        class: "button"
      ) %>
    </div>
  <% end %>
  <%= render_collection(
    partial: "logs/log",
    collection: @logs,
    as: :log,
    locals: {
      nested: []
    }
  ) %>
<% end %>
