<% content_for(:title, @job_claimed_execution) %>

<%= turbo_stream_from(@job_claimed_execution) %>

<% if @job_claimed_execution.id.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".id") %></div>
    <div class="font-bold"><%= @job_claimed_execution.id %></div>
  </div>
<% end %>

<% if @job_claimed_execution.job.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".job") %></div>

    <% if can?(:show, @job_claimed_execution.job) %>
      <div class="font-bold">
        <%= link_to(
          @job_claimed_execution.job,
          [*nested(job: nil), @job_claimed_execution.job],
          class: "link"
        ) %>
      </div>
    <% else %>
      <div class="font-bold"><%= @job_claimed_execution.job %></div>
    <% end %>
  </div>
<% else %>
  <div class="p">
    <div class="text-gray-600"><%= t(".job_id") %></div>
    <div class="font-bold"><%= @job_claimed_execution.job_id %></div>
  </div>
<% end %>

<% if @job_claimed_execution.process_id.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".process_id") %></div>
    <div class="font-bold"><%= @job_claimed_execution.process_id %></div>
  </div>
<% end %>

<% if @job_claimed_execution.updated_at.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".updated_at") %></div>
    <div class="font-bold">
      <%= local_time(@job_claimed_execution.updated_at) %>
    </div>
  </div>
<% end %>

<% if @job_claimed_execution.created_at.present? %>
  <div class="p">
    <div class="text-gray-600"><%= t(".created_at") %></div>
    <div class="font-bold">
      <%= local_time(@job_claimed_execution.created_at) %>
    </div>
  </div>
<% end %>

<div class="p">
  <%= link_to(t(".edit"), edit_url, class: "link") %>
  <%= render("shared/confirm", text: t(".destroy"), url: destroy_url) %>
  <%= render("shared/confirm", text: t(".delete"), url: delete_url) %>
</div>
