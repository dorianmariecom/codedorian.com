<div class="p">
  <% parents = nested.compact %>
  <% query =
    parents.to_h do |parent|
      ["#{parent.model_name.singular_route_key}_id", parent.to_param]
    end %>
  <%= link_to([submission_section, **query], class: :link) do %>
    <%= submission_section %>
  <% end %>
</div>
