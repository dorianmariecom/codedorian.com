<% content_for(:title, @attachment) %>

<%= turbo_stream_from(@attachment) %>

<%= render("shared/attribute", label: t(".id"), value: @attachment.id) %>

<%= render(
  "shared/attribute",
  label: t(".user"),
  value: @attachment.user,
  type: :record
) %>

<% if @attachment.file.attached? %>
  <div class="p">
    <div class="text-gray-600">
      <%= link_to(t(".preview"), [*show_url, :preview], class: :link) %>
    </div>
  </div>

  <div class="p">
    <div class="text-gray-600">
      <%= link_to(t(".download"), [*show_url, :download], class: :link) %>
    </div>
  </div>
<% end %>

<%= render(
  "shared/attribute",
  label: t(".updated_at"),
  value: @attachment.updated_at,
  type: :time
) %>

<%= render(
  "shared/attribute",
  label: t(".created_at"),
  value: @attachment.created_at,
  type: :time
) %>

<div class="p p--flex">
  <%= link_to(t(".edit"), edit_url, class: :link) %>
  <%= render("shared/confirm", text: t(".destroy"), url: destroy_url) %>
  <%= render("shared/confirm", text: t(".delete"), url: delete_url) %>
</div>
